name: Erlang CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    runs-on: ubuntu-latest

    container:
      image: erlang:22.0.7

    steps:
    - uses: actions/checkout@v2
      
    - name: Compile
      working-directory: ${{github.workspace}}/task_5
      run: rebar3 compile
      
    - name: Run tests
      working-directory: ${{github.workspace}}/task_5
      run: rebar3 do eunit
